#!/usr/bin/env bash

set -euo pipefail

cat <<EOF
No additional configuration is required. The plugin automatically detects
your platform (Linux/macOS) and architecture (amd64/arm64) to download
the correct binary release.

Optional environment variable for API rate limiting:
- GITHUB_API_TOKEN: GitHub personal access token for authenticated requests
EOF
